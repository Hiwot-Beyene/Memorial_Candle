import 'package:flutter/material.dart';

class MyCandles extends StatelessWidget {
  const MyCandles({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mycandles Page"),);
  }
}